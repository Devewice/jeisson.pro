/** Fondo ambiental. false = sin fondo animado. */
export const ENABLE_AMBIENT_BACKGROUND = true

/**
 * full — animación CSS completa
 * lite — solo transform/opacity (sin grain ni brillo pulsante; más fluido)
 * static — gradiente fijo, cero animación (máximo rendimiento)
 */
export const AMBIENT_MODE = 'lite'
